# Only needed when running locally (set automatically on the Apify platform).
APIFY_TOKEN=

# Optional: official Meta Ad Library API (adsProvider "meta-graph").
META_ACCESS_TOKEN=

# Optional: LLM enrichment (useLlm: true). Read as a fallback for llmApiKey.
ANTHROPIC_API_KEY=
